# Day 1 - HTML/CSS Webpage

Create two files `index.html` and `styles.css` in the project directory and create a webpage which looks like this:

![](fullpage_html_css.png)

# Final Result

![](final.png)
